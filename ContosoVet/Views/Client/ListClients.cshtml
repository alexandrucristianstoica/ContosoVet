@model ContosoVet.ViewModels.ListClientsViewModel;

<br>
<br>
<br>
<br>
<div class="card-body">
    <a class="btn btn-success" asp-controller="Client"
       asp-action="Client"
       role="button">Add client</a>
</div>

<table class="table">
    <thead>
        <tr>
            <th class="text-center">Id</th>
            <th><span style="font-size:24px"></span>Name</th>
            <th>Phone Number</th>
            <th>Email</th>
            <th>Address</th>
            <th class="text-center">Actions</th>
        </tr>
    </thead>
    @foreach (var client in Model.Clients)
    {
        <tbody>
            <tr>
                <td class="text-center">@client.Id</td>
                <td>@client.Name</td>
                <td>@client.PhoneNumber</td>
                <td>@client.Email</td>
                <td>@client.Address</td>
                <td class="td-actions text-center">
                    <button type="button" rel="tooltip" class="">
                        <a asp-controller="Client"
                           asp-action="Client"
                           asp-route-id="@client.Id"><span style="color:indianred"></span>Update</a>
                    </button>
                    <button type="button" rel="tooltip" class="">
                        <a asp-controller="Client"
                           asp-action="Delete"
                           asp-route-id="@client.Id"><span style="color:darkred"></span>Delete</a>
                    </button>
                </td>
            </tr>
        </tbody>
    }
</table>

